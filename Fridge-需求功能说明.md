# Fridge 需求功能说明

## 1. 产品定位

`Fridge` 是一个便携的 Windows FRP 部署工具，用来简化 FRP + Windows 远程桌面的搭建过程。

首版只解决两件事：

1. 用户输入 Linux 服务器连接信息，工具通过 SSH 自动部署 FRPS。
2. 用户点击按钮，在当前 Windows 电脑部署 FRPC、启用 RDP 并配置开机运行。

工具不做项目管理平台、云厂商控制台或自研远控客户端。目标是保持界面简单、操作直接，并尽量减少用户手动复制文件、编辑配置和执行命令的步骤。

## 2. 目标用户与环境

- 用户拥有一台可通过 SSH 登录的公网 Linux 服务器。
- Linux 服务器使用 systemd，CPU 架构为 amd64 或 arm64。
- SSH 账号为 root，或能够免密执行 sudo。
- 被控电脑为 Windows 10/11 x64，并具备可用的 RDP 服务端能力。
- 部署当前电脑时，用户需要以管理员权限运行 Fridge。

## 3. 核心流程

### 3.1 输入连接配置

界面提供以下字段：

- 服务器地址：IPv4 地址或主机名。
- SSH 端口：默认 `22`，范围 `1-65535`。
- SSH 用户名：默认 `root`。
- SSH 密码。
- FRPS 端口：默认 `7000`，范围 `1-65535`。
- 公网 RDP 端口：默认 `3389`，范围 `1-65535`。
- 认证 Token：自动生成，也可重新生成或手动修改。

FRPS 端口和公网 RDP 端口不能相同。

### 3.2 部署云服务器

点击“部署服务器”后，工具应：

1. 校验服务器、SSH、端口和 Token 配置。
2. 通过 SSH 连接服务器。
3. 显示服务器主机密钥指纹，并要求用户确认。
4. 检测 Linux CPU 架构。
5. 检查服务器已有的 FRPS systemd 服务、进程、可执行文件和目标端口。
6. 如果检测到已有 Fridge FRPS，则允许更新；检测到其他 FRPS 或端口冲突时，在写入前中止并显示具体原因。
7. 上传工具内嵌的 FRPS 安装包和部署脚本。
8. 写入 FRPS 配置和 systemd 服务。
9. 启动服务并检查运行状态。
10. 在界面日志中显示执行进度和错误信息。

### 3.3 部署当前电脑

点击“部署本机被控端”后，工具应：

1. 校验服务器地址、FRPS 端口、公网 RDP 端口和 Token。
2. 检查管理员权限、已有的 `frpc.exe` 进程、计划任务和常见旧安装目录。
3. 如果检测到已有 Fridge FRPC，则允许更新；检测到其他 FRPC 或计划任务时，在写入前中止并显示具体原因。
4. 将内嵌的 `frpc.exe` 释放到固定安装目录。
5. 生成与服务器配置一致的 `frpc.toml`。
6. 启用 Windows RDP、NLA、TLS 和相应防火墙规则。
7. 注册以 SYSTEM 身份运行的开机计划任务，默认延迟 30 秒，避开 DHCP 和网络初始化阶段。
8. 通过隐藏 watchdog 启动 FRPC；FRPC 异常退出或开机时服务器暂不可达时，每 15 秒自动重试。
9. 将 FRPC 启动、退出码和重试过程写入受控大小的本地日志，并检查进程是否正常运行。
10. 在界面日志中显示执行进度和错误信息。

## 4. 界面与交互要求

- 界面使用单窗口，不增加多层向导和复杂导航。
- 连接配置、服务器部署、本机部署和执行日志在同一界面展示。
- 控件垂直对齐，间距统一，输入框、卡片和按钮使用适当圆角。
- 输入框获得焦点时显示清晰的焦点边框。
- 点击当前焦点控件之外的任意位置时，输入框应失去焦点。
- 空输入框失焦时不校验，也不显示错误。
- 非空输入框失焦时才执行格式校验并显示错误。
- 点击部署按钮时执行完整的必填项与格式校验。
- 端口使用普通数字文本框，不显示右侧增减按钮。
- 密码和 Token 默认隐藏，并提供显示选项。
- 操作执行期间禁用重复部署，提供取消按钮。

## 5. 校验规则

- 服务器地址必须是有效 IPv4 地址或主机名。
- SSH 用户名在服务器部署时不能为空。
- SSH 密码在服务器部署时不能为空。
- 所有端口必须为 `1-65535` 之间的整数。
- FRPS 端口与公网 RDP 端口不能相同。
- Token 长度为 16-128，只允许字母、数字、点、下划线和连字符。
- 输入错误通过红色边框、错误图标和底部状态文本提示。
- 用户修改为有效内容后，应及时清除对应错误状态。

## 6. 打包与分发

- 发布目标为 Windows x64。
- 使用自包含单文件发布，目标电脑无需预装 .NET。
- FRPC、Linux amd64/arm64 FRPS 包和部署脚本全部编译进 `Fridge.exe`。
- 最终用户只需要复制 `publish/Fridge.exe`，不需要复制源码、Assets 或 Legacy。
- `Legacy` 仅保存旧版手工部署教程和脚本，可随时删除，不得被项目构建或程序运行引用。

## 7. 安全边界

- 工具不自动修改云厂商安全组；部署完成后提示用户放行端口。
- 首次 SSH 连接必须由用户确认主机密钥指纹。
- 密码和 Token 不写入普通执行日志。
- watchdog 日志不得记录 Token，并限制日志大小和保留数量。
- 本机系统配置必须在管理员权限下执行。
- 工具只配置用户明确提供的服务器和当前 Windows 电脑。

## 8. 首版不包含

- 多服务器、多设备或项目列表管理。
- 云厂商 API 和安全组自动修改。
- SSH 私钥登录和凭据持久化。
- 自动生成或启动 `.rdp` 文件。
- 图形化诊断中心、证书管理、自动升级和回滚界面。
- 自研 RDP 客户端、远控协议或中继服务。

以上能力只有在轻量部署流程稳定后，再根据实际使用需求逐项增加。

## 9. 验收标准

- 在干净的 Windows x64 电脑上，仅复制 `Fridge.exe` 即可启动。
- 输入有效服务器信息后，可以通过 UI 完成 Linux FRPS 部署。
- 以管理员身份运行时，可以完成当前电脑 FRPC、RDP 和开机任务配置。
- 开机时网络晚于任务计划程序就绪，或运行中的 FRPC 被强制结束时，watchdog 能自动重试并恢复到 FRPS 的连接。
- 删除 `Legacy` 目录后，项目仍能成功构建并发布，程序功能不受影响。
- 空字段失焦不报错，非空无效字段失焦正确报错，部署时缺失必填项正确报错。
- 端口输入框无增减按钮，并能阻止键盘输入非数字字符。
- 发布构建无警告、无错误。
